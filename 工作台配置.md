# 工作台配置 · 小贝壳记 🐚

> 这份文件是给 AI 看的。以后想改工作台，直接跟 AI 说「给我的工作台加个喝水提醒」，
> 它会读这份配置接着改，不用重新聊一遍。**请不要删掉这个文件。**

生成时间：2026-09-06
最后更新：2026-09-06（把「健身」改为「运动」）

---

## 一、用户画像

（访谈第 1 轮的结论，一两句话，决定了以后推荐什么功能）

- **身份 / 主要在忙什么**：自媒体 + 吃药 + 运动 + 做项目（短片、小红书图文、AI 创作）
- **核心诉求**：每天在手机打开就能看到、隐私数据不出本机、能长期坚持记账 + 灵感
- **隐私敏感度**：高——账目 / 灵感 / 笔记 / 吃药都属于私人数据，明确要求不部署到公网
- **技术水平**：会一点。能区分 AI 创作提示词与日常工具用法，长期维护飞书知识库，工作区按类型分文件管理；不算程序员

---

## 二、功能清单

| # | 功能 | 标识 | 类型 | 侧边栏 | 说明 |
|---|---|---|---|---|---|
| 1 | 今日仪表盘 | `today` | 本地 | 🐚 今天 | 聚合展示吃药/运动/待办/账目/灵感 |
| 2 | 吃药打卡 | `med` | 本地 | 💊 吃药 | 药品 + 时点 + 每日重置勾选状态 |
| 3 | 运动打卡 | `gym` | 本地 | 🏃 运动 | 月度目标 + 每日打卡 + 体重曲线（SVG 手画） |
| 4 | 项目看板 | `projects` | 本地 | 🎬 项目 | 项目名 + 状态 + 待办清单 |
| 5 | 日程待办 | `todo` | 本地 | 📅 日程 | 简单时间块 + 勾选 |
| 6 | 账目 | `money` | 本地 | 💰 账目 | 收支分类 + 月度趋势图（SVG 手画） |
| 7 | 灵感速记 | `idea` | 本地 | 💡 灵感 | 一句话 + 时间戳 + 标签 |
| 8 | 自由笔记 | `notes` | 本地 | 📝 笔记 | 多篇长文，markdown 预览 |
| 9 | 周复盘 | `recap` | 本地 | 🌙 复盘 | 每周一自动拉上周数据生成小卡片 |
| 10 | 备份/恢复 | `settings` | 本地 | ⚙️ 设置 | 一键导出 JSON / 导入 / 清空 |
| - | 设置 | `settings` | 本地 | ⚙️ 设置 | 备份 / 清空 / 重置 |

**用户明确拒绝过的功能**（以后别再推荐）：
- 天气（手机自带）
- 双端同步（用户选择单设备，数据只在本机）

---

## 三、风格参数

选定版本：**B · 森系 × 日系混搭**

```css
--bg: #f5efe4;            /* 奶油米色，页面底色 */
--sidebar: #7d9471;       /* 森系绿，左侧栏 */
--card: #fdfaf4;          /* 象牙白，卡片背景 */
--text: #3d352c;          /* 暖棕，正文文字 */
--text-dim: #8b7d6a;      /* 次要文字 */
--accent: #6b8a55;        /* 暖绿，强调按钮/线 */
--radius: 16px;
--shadow: 0 2px 12px rgba(120,100,70,.10);
--font: 'Noto Sans SC', 'PingFang SC', system-ui, sans-serif;
```

- 品牌 emoji：🐚
- 工作台名：小贝壳记
- 短名称（桌面图标下的字）：小贝壳记（4 字）
- 图标做法：纯色 + 文字（森系绿底 + 「贝」字，512×512，make_icon.py 生成）
- 用户在预览环节提过的偏好：
  - 倾向日系/古风/胶片质感
  - 圆角中等，不要太花
  - 字体最终选了圆体（Noto Sans SC）
  - 配色不要大红大紫，森林感 + 米色基底

---

## 四、数据结构

存储命名空间：`bysdash:`（敏感数据 `bysdash$secret:`，不进备份）
当前没有任何联网功能，所以没有 secret。

| Store key | 结构 | 归属功能 |
|---|---|---|
| `med_template` | `[{id, name, times: ['07:00','22:00']}]` | 吃药打卡 |
| `med_done:YYYY-MM-DD` | `[{medId, time}]` | 吃药打卡（按天） |
| `gym_goal` | `number`（默认 25） | 运动打卡 |
| `gym_done:YYYY-MM-DD` | `boolean` | 运动打卡（按天） |
| `weight` | `[{date, value}]` | 运动打卡（体重曲线） |
| `projects` | `[{id, name, status, todos: [{id, text, done}]}]` | 项目看板 |
| `todo_template` | `[{id, text}]` | 日程待办 |
| `todo_done:YYYY-MM-DD` | `[itemId]` | 日程待办（按天） |
| `money` | `[{id, date, amount, type: 'in'/'out', category, note}]` | 账目 |
| `ideas` | `[{id, text, ts, tags: []}]` | 灵感速记 |
| `notes` | `[{id, title, body, ts}]` | 自由笔记 |
| `_lastBackup` | `timestamp` | 系统 |
| `_lastWrite` | `timestamp` | 系统 |
| `_lastPage` | `string` | 系统 |

---

## 五、联网配置

**无。**

工程里**没有任何**外部 API 调用，账号注册、实名认证、API key 全不涉及。已确认 `apiCard` 已从设置页删除。

---

## 六、部署

- **本地目录**：`E:\Desktop\早睡身体好\AI创作\我的项目\个人工作台\小贝壳记\`
- 层级：**L0 · 未部署**（用户在第 5 步默认走 B = PWA 部署，但还没明确说要不要现在就部署）
- 平台：EdgeOne Pages（待定，未上线）
- 地址：无
- 部署方式：拖整个文件夹上传
- 已加到桌面：否
- 云同步配置：无（用户选择单设备）

> **下一步提示**：等用户下次说「部署」或「我手机怎么用」，再做 PWA 部署。手机端首次访问后浏览器会弹出「添加到主屏幕」，加完像 App 一样用。

---

## 七、工程状态

- 文件结构：**三文件**（`index.html` + `manifest.json` + `icon.png`）
- index.html 行数：**1471 行**（接近但未到 1500 阈值，暂不拆 `features/`）
- 上次验收脚本：通过 · 2026-09-06
  - 静态检查：`validate_dashboard.py` —— 0 阻塞 + 2 个 warn（都是建议）
  - 冒烟测试：`smoke_test.js` —— 9/9 通过
  - 改名回归：把「健身」→「运动」后 9/9 仍通过

---

## 八、变更记录

| 日期 | 改了什么 |
|---|---|
| 2026-09-06 | 初始生成，9 个功能 + 1 个仪表盘，森系×日系混搭，圆体，隐私级 L0 |
| 2026-09-06 | 把「健身」全部改为「运动」（侧栏、页面标题、提示文案、注释、备份文档），内部 id 保持不变，旧数据兼容 |
| 2026-09-06 | 图标文字由「小」改为「贝」，森系绿底，60px 预览可辨识 |