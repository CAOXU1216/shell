# 工作台配置 · 小贝壳记 🐚

> 这份文件是给 AI 看的。以后想改工作台，直接跟 AI 说「给我的工作台加个喝水提醒」，
> 它会读这份配置接着改，不用重新聊一遍。**请不要删掉这个文件。**

生成时间：2026-09-06
最后更新：2026-09-07（按代码实际情况重写：存储键、功能清单、工程状态全部对齐）

---

## 一、用户画像

（访谈第 1 轮的结论，一两句话，决定了以后推荐什么功能）

- **身份 / 主要在忙什么**：自媒体 + 吃药 + 运动 + 做项目（短片、小红书图文、AI 创作）
- **核心诉求**：每天在手机打开就能看到、隐私数据不出本机、能长期坚持记账 + 灵感
- **隐私敏感度**：高——账目 / 灵感 / 笔记 / 吃药都属于私人数据，明确要求不部署到公网
- **技术水平**：会一点。能区分 AI 创作提示词与日常工具用法，长期维护飞书知识库，工作区按类型分文件管理；不算程序员

---

## 二、功能清单

⚠️ 标识（第二列）就是代码里的 `App.routes[].id`，改功能时以它为准，不要另起名字。

| # | 功能 | 标识 | 类型 | 侧边栏 | 说明 |
|---|---|---|---|---|---|
| 1 | 仪表盘 | `today` | 本地 | 🐚 今天 | 聚合吃药 / 运动 / 日程 / 账目 / 灵感；卡片可排序、可隐藏 |
| 2 | 吃药打卡 | `med` | 本地 | 💊 吃药 | 药品 + 时点，按天重置勾选 |
| 3 | 运动打卡 | `gym` | 本地 | 🏃 运动 | 月度目标 + 每天可多次打卡（每条带时长分钟 + 备注）+ 体重曲线（SVG 手画），日历格子显示当日累计分钟 |
| 4 | 项目看板 | `projects` | 本地 | 🎬 项目 | 项目名 + 状态 + 待办清单 |
| 5 | 日程待办 | `todo` | 本地 | 📅 日程 | 当天的待办，勾选 / 删除，**按天存储** |
| 6 | 账目 | `money` | 本地 | 💰 账目 | 收支分类 + 月度趋势图（SVG 手画） |
| 7 | 灵感速记 | `inbox` | 本地 | 💡 灵感 | 一句话 + 时间戳 + `#标签` |
| 8 | 自由笔记 | `notes` | 本地 | 📝 笔记 | 多篇文章，markdown 预览 |
| 9 | 周复盘 | `recap` | 本地 | 🌙 复盘 | 每周一自动拉上周数据；可导出 1080×1350 PNG 图卡 |
| 10 | 设置 / 备份 | `settings` | 本地 | ⚙️ 设置 | 导出 JSON / 导入 / 清空；侧栏排序·隐藏·锁定；今日卡片排序·隐藏 |

**设置页里藏着的三个能力**（不是独立页面，别当成缺失功能去找）：
- 侧栏自定义：`_navOrder` / `_navHidden` / `_navEditable`
- 今日卡片自定义：`_todayOrder` / `_todayHidden` / `_todayEditable`
- 备份提醒：根据 `_lastBackup` 与 `_lastWrite` 比较，提示该导出了

**用户明确拒绝过的功能**（以后别再推荐）：
- 天气（手机自带）
- 双端同步（用户选择单设备，数据只在本机）

---

## 三、风格参数

选定版本：**B · 森系 × 日系混搭**

```css
--bg: #f5efe4;            /* 奶油米色，页面底色 */
--sidebar: #7d9471;       /* 森系绿，左侧栏 */
--card: #fdfaf4;          /* 象牙白，卡片背景 */
--text: #3d352c;          /* 暖棕，正文文字 */
--text-dim: #8b7d6a;      /* 次要文字 */
--accent: #6b8a55;        /* 暖绿，强调按钮/线 */
--radius: 16px;
--shadow: 0 2px 12px rgba(120,100,70,.10);
--font: 'Noto Sans SC', 'PingFang SC', system-ui, sans-serif;
```

- 品牌 emoji：🐚
- 工作台名：小贝壳记
- 短名称（桌面图标下的字）：小贝壳记（4 字）
- 图标做法：纯色 + 文字（森系绿底 + 「贝」字，512×512，make_icon.py 生成）
- 用户在预览环节提过的偏好：
  - 倾向日系/古风/胶片质感
  - 圆角中等，不要太花
  - 字体最终选了圆体（Noto Sans SC）
  - 配色不要大红大紫，森林感 + 米色基底

---

## 四、数据结构

存储命名空间：`bysdash:`（敏感数据 `bysdash$secret:`，不进备份）
当前没有账号体系，所以没有 secret。

⚠️ 这一节 2026-09-07 按代码重写过，**旧版本里写的 `med_template` / `weight` / `ideas` / `todo_template` 都是错的，已经不存在了**。

| Store key | 结构 | 归属功能 |
|---|---|---|
| `med_plan` | `[{id, name, times: ['07:00','22:00']}]` | 吃药打卡（药品模板，长期） |
| `med_taken:YYYY-MM-DD` | `[{id: '药品id@时点'}]` | 吃药打卡（按天，getDaily） |
| `gym_goal` | `number`（默认 25，单位：天/月） | 运动打卡 |
| `gym_done:YYYY-MM-DD` | `[{id, ts, dur, note, _u}]`（数组，一天可多次；`dur` 是分钟数，缺省 30，旧数据自动补；`ts` 是 ms 时间戳） | 运动打卡（按天） |
| `weight_log` | `[{id, date, value}]` | 运动打卡（体重曲线） |
| `projects` | `[{id, name, status, todos: [{id, text, done}], _u}]` | 项目看板 |
| `todo_items:YYYY-MM-DD` | `[{id, text, done}]` | 日程待办（按天，getDaily） |
| `money` | `[{id, date, amount, category, memo, _u}]`，`amount` 带符号（支出为负） | 账目 |
| `inbox` | `[{id, text, tags: [], _u}]` | 灵感速记 |
| `notes` | `[{id, title, content, _u}]` | 自由笔记 |
| `recap_done:YYYY-MM-DD` | 计数器（内部存事件列表，用 `incrDaily/decrDaily` 读写） | 周复盘 |
| `_streak` | **已废弃**（2026-09-07）：只在 `Recap._streak()` 里读 0，现在改为从 `recap_done:日期` 自动推算连续天数 | 旧键留在 localStorage 不影响运行，下次清空数据时会被 `wipe()` 一起清掉 |
| `_navOrder` | `[routeId]` | 设置（侧栏顺序） |
| `_navHidden` | `[routeId]` | 设置（侧栏隐藏） |
| `_navEditable` | `boolean` | 设置（侧栏是否允许编辑） |
| `_todayOrder` | `[cardId]` | 设置（今日卡片顺序） |
| `_todayHidden` | `[cardId]` | 设置（今日卡片隐藏） |
| `_todayEditable` | `boolean` | 设置（今日卡片是否允许编辑） |
| `_lastBackup` | `timestamp` | 系统（备份提醒用） |
| `_lastWrite` | `timestamp` | 系统（Store 每次写入自动更新） |
| `_lastPage` | `string` | 系统（记住上次打开的页面） |

**Store 用法约定**（改代码时必须遵守）：
- 列表数据一律 `Store.list/upsert/softDelete`，删除是软删除（墓碑 30 天后清）
- 按天数据用 `Store.getDaily/setDaily`，会自动拼上今天日期
- 计数用 `Store.incrDaily/decrDaily`，不要用 get→+1→set（那样双端合并会丢数据）
- 所有用户输入渲染前必须过 `Util.esc()`

---

## 五、联网配置

**没有 API 调用。** 没有账号、没有 key、没有后端。

唯一的对外请求是**字体**（不是数据）：

```html
<link href="https://fonts.googleapis.com/css2?family=Noto+Sans+SC:wght@400;500;700&display=swap" rel="stylesheet">
```

- 加载失败时回退到 `PingFang SC / system-ui`，页面不会坏，只是字形变一下
- 国内网络经常加载不到，属于已知问题（见第七节待处理）
- 除此之外工程里没有任何 `fetch` / `XMLHttpRequest` / 第三方脚本

---

## 六、部署

- **本地目录**：`E:\Desktop\早睡身体好\AI创作\我的项目\个人工作台（正式版）\小贝壳记\`
- 层级：**L0 · 未部署**（用户在第 5 步默认走 B = PWA 部署，但还没明确说要不要现在就部署）
- 平台：EdgeOne Pages（待定，未上线）
- 地址：无
- 部署方式：拖整个文件夹上传（注意排除 `.workbuddy/` 和 `.git/`）
- 已加到桌面：否
- 云同步配置：无（用户选择单设备）

> **下一步提示**：等用户下次说「部署」或「我手机怎么用」，再做 PWA 部署。手机端首次访问后浏览器会弹出「添加到主屏幕」，加完像 App 一样用。

---

## 七、工程状态

- 文件结构：**三文件**（`index.html` + `manifest.json` + `icon.png`）
- index.html 行数：**2265 行**（已超过 1500 拆分线，建议下次大改时拆 `features/`）
- 分区标记：**20 个**（HTML 10 对 + JS 10 对，名字一一对应：仪表盘 / 吃药打卡 / 运动打卡 / 项目看板 / 日程待办 / 账目 / 灵感速记 / 自由笔记 / 周复盘 / 设置）
  - 格式：HTML `<!-- ==== 功能：X START ==== -->`，JS `/* ==== 功能：X START ==== */`
  - 新功能插在 `<!-- ==== 功能：设置 START ==== -->` 之前，JS 插在 `/* ==== 功能：设置 START ==== */` 之前
- 上次验收：2026-09-07
  - `validate_dashboard.py`：**0 阻塞 + 3 个建议**（侧栏 10 个偏多、缺英雄元素、文件偏长）
  - JS 语法检查：通过

**待办（用户 2026-09-07 看过，决定先不做）**

⚠️ 这四项都已经跟用户讲清楚影响和代价了，**用户主动选择暂缓**。不要反复推荐，等用户自己提再做。

| # | 待办 | 影响（不做会怎样） | 做法 | 什么时候做 |
|---|---|---|---|---|
| 1 | 加 service worker | 断网 / 信号差时可能白屏（数据不丢，只是壳子加载不出来） | 新增 `sw.js`（约 20 行）+ index.html 里注册；**装了之后更新要刷新两次才生效** | 用户说「做 PWA」「部署」「断网也要能用」「加桌面」时，和 2、3 一起做 |
| 2 | 字体本地化 | 国内经常取不到 Google 字体，字形偶发跳变；也是唯一外链 | 删掉 Google Fonts 外链，改用 `PingFang SC / system-ui` 系统字体栈 | 同 1 |
| 3 | `manifest.json` 描述改「健身」→「运动」 | 加到桌面时显示旧文案 | 改一个词 | 同 1，或下次部署前顺手改 |
| 4 | 单文件 2265 行 | 后续迭代容易改错地方 | 按 build-guide.md 拆 `features/` | 下次大改功能时再说，不用单独做 |

> 2026-09-07 已从 `assets/icons/` 移走 10 个未被引用的 PNG（约 570KB），
> 备份在 `.workbuddy/backup/assets_icons/`，确认不需要后可删。

---

## 八、变更记录

| 日期 | 改了什么 |
|---|---|
| 2026-09-06 | 初始生成，9 个功能 + 1 个仪表盘，森系×日系混搭，圆体，隐私级 L0 |
| 2026-09-06 | 把「健身」全部改为「运动」（侧栏、页面标题、提示文案、注释、备份文档），内部 id 保持不变，旧数据兼容 |
| 2026-09-06 | 图标文字由「小」改为「贝」，森系绿底，60px 预览可辨识 |
| 2026-09-07 | 补 JS 分区标记 10 对（HTML 原本就有，JS 缺），名字与 HTML 完全对齐 |
| 2026-09-07 | 清理 `assets/icons/` 下 10 个零引用 PNG，移到 `.workbuddy/backup/assets_icons/` |
| 2026-09-07 | 按代码实际情况重写本文件：存储键、功能清单、行数、部署路径全部更新，新增「待处理」表 |
| 2026-09-07 | 「待处理」改为「待办」并标注状态：PWA 三件（sw.js / 字体本地化 / manifest 措辞）与文件拆分，用户已知悉并决定暂缓 |
| 2026-09-07 | 修 `_streak` 永远为 0 的 bug：改为 `Recap._streak()` 从 `recap_done:日期` 自动推算连续天数；同批清掉 `.mono`+`--mono`、`.tag.rem`、`Store.listDaily/upsertDaily/countDaily`、`#medPlanCard` |
| 2026-09-07 | 运动打卡加时长（分钟）字段 `dur`：打卡按钮旁小输入框默认 30、范围 1-600；日历格子下方显示当日累计分钟（≥100 显示 `Xh`，<100 显示 `Xm`）；记录行显示"X分 · 备注"；dashboard 摘要改"今日已运动 X 分钟（N 次）"；旧数据 `dur` 缺省自动补 30 |
